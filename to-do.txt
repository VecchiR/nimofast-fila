BACKEND:
    - conferir status codes 
        - implementar status' do fiber? (fiber.StatusOK, fiber.StatusBadRequest,....)

    - handlers/motoristas.go: 
        - quebrar a funçao "Buscar"? (separar a busca de cpf e placa) -> a funçao faz tudo dentro dela

    - db e models: 
        - ter uma tabela de "veículos", e na tabela de "motoristas" trocar a coluna "placa" por um link com veículos, permitindo que um motorista vincule diferentes veículos, e salvar o id do veiculo na entrada, assim como o id do motorista

- validação modelos de placa LLLNNNN ou LLLNLNN (L= LETRA, N= NUMERO)
    - MAS É DIFERENTE DEPENDENDO DO PAIS DO MERCOSUL..... (argentina = LLNNNLL)
        - MAS não importa nesse projeto pq o motorista tem que ter CPF e CNH....


Implementar POLLING com período configurável!


historico -> "tempo total na base" e "horario finalização" ->  vai dar pra calcular para o FINALIZADO.... mas CANCELADO não armazenamos horário de finalização (só fim_carregamento)
    teria que consertar isso na modelagem primeiro